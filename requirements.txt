# ============================================================
#  ربات ری‌پست هوشمند MR LiQ - وابستگی‌های کامل
# ============================================================

# ---------------- هسته‌ی ربات (همیشه لازم) ----------------
python-telegram-bot[job-queue]==21.6
httpx==0.27.2
# ---------------- سرورِ API برای اکستنشنِ مرورگر ----------------
aiohttp==3.10.10
beautifulsoup4==4.12.3
lxml==5.3.0
Pillow==10.4.0
numpy==1.26.4
arabic-reshaper==3.0.0
python-bidi==0.4.2
python-dotenv==1.0.1

# ---------------- پردازش تصویر و واترمارک ----------------
# OpenCV برای حذف واترمارک و تشخیص الگو
opencv-python-headless==4.10.0.84

# ---------------- هوش مصنوعی (بهبود کیفیت + LaMa) ----------------
# Torch برای اجرای مدل‌های Real-ESRGAN و LaMa
# از نسخه CPU استفاده می‌شود (بدون نیاز به CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1

# ---------------- تاریخ شمسی (جلالی) ----------------
jdatetime==4.1.1
# دیتابیسِ منطقه‌های زمانی (IANA) - بدونِ این، ZoneInfo("Asia/Tehran") روی بعضی
# سرورهای مینیمال/کانتینری (که دیتابیسِ tz سیستم رو ندارن) خطا می‌ده.
tzdata==2024.1

# ---------------- رمزنگاری برای بکاپ ----------------
cryptography==42.0.5

# ---------------- مانیتورینگ منابع سرور ----------------
psutil==5.9.8

# ---------------- عملیات فایل‌های غیرهمزمان ----------------
aiofiles==23.2.1

# ---------------- خروجیِ اکسلِ گزارشِ فیدبکِ فیلترِ تبلیغات ----------------
openpyxl==3.1.5

# (ماژولِ «تبلیغات» هیچ وابستگیِ اضافه‌ای نداره؛ فقط از python-telegram-bot که بالاست استفاده می‌کنه)

# ---------------- SDKهای رسمی هوش مصنوعی (اختیاری) ----------------
# در صورتی که بخواهید به‌جای httpx از SDK رسمی استفاده کنید
# mistralai>=1.0.0
# groq>=0.9.0