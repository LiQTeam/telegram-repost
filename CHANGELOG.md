# CHANGELOG — MR LiQ Telegram Repost Bot

## نسخه v2.4.2 — ۱۴۰۵/۰۵/۰۸ (2026-07-30) — وضعیتِ زنده‌ی همه‌ی موتورها + پاک‌سازیِ منوها

### «🧠 وضعیتِ زنده‌ی موتورهایِ AI» حالا همه‌ی سرویس‌ها را می‌بیند
قبلاً این صفحه فقط **Groq و Mistral**ِ `.env` را تست می‌کرد
(`ai_router.check_status`). یعنی **Google Gemini** و **Hugging Face** و کلاً
هر کلیدِ شخصی‌ای که از منویِ «🔌 مدیریتِ API هوش مصنوعی» ثبت شده بود اصلاً در
گزارش دیده نمی‌شد — درحالی‌که همان صفحه بالاتر هر ۴ سرویس را با آیکونِ 🟢
فهرست می‌کرد. نتیجه: دو صفحه دو حرفِ متفاوت می‌زدند.

حالا `ai_provider_manager.live_status_report()` تک‌منبعِ حقیقتِ وضعیت است:
- **همه‌ی ۴ سرویسِ کاتالوگ × همه‌ی کلیدهاشون** (تا ۵ کلید برای هرکدام) واقعاً
  تست می‌شوند، به‌علاوه‌ی کلیدِ `.env` برای سرویس‌هایی که کلیدِ ذخیره‌شده ندارند.
- تست‌ها **موازی** اجرا می‌شوند (`asyncio.gather`) با سقفِ کلیِ ۲۵ ثانیه —
  سریالی می‌توانست تا ۴ دقیقه طول بکشد (۲۰ کلید × ۱۲ ثانیه).
- نتیجه‌ی هر کلید **در دیتابیس هم نوشته می‌شود**، پس آیکونِ صفحه‌ی «مدیریتِ
  API» دقیقاً همان چیزی را نشان می‌دهد که گزارشِ زنده گفته.
- کلیدی که همان لحظه به Quota خورده، مثلِ مسیرِ عادیِ فراخوانی یک
  `cooldown` واقعی می‌گیرد. (بدونِ این، `_aggregate_status` کلیدِ
  quota-خورده با کولداونِ خالی را «فعال» حساب می‌کرد و آیکون 🟢 می‌شد —
  دقیقاً برعکسِ چیزی که همان گزارش نشان داده بود.)
- گزارش برایِ هر سرویس مدلِ پیش‌فرض، قابلیت‌ها (📝/🖼)، وضعیتِ تک‌تکِ
  کلیدها با دلیلِ خطا، و جمعِ کلِ کلیدهای فعال را نشان می‌دهد. سرویسِ بدونِ
  کلید ⚪️ «ثبت نشده» است، نه ❌ خطا.

### پاک‌سازیِ منوها
- **«👁 Vision» و «🔤 OCR» از «🔀 مسیریابیِ وظایف» حذف شدند.** این دو
  `task_id` هیچ‌جای ربات صدا زده نمی‌شدند (نه `ai_router`، نه `image_router`،
  نه هیچ هندلری) و فقط دو ردیفِ بی‌اثر بودند که کاربر می‌توانست برایشان
  سرویس تنظیم کند بدونِ اینکه هرگز استفاده شود. چون منویِ مسیریابی از
  `ai_catalog.ALL_TASKS` ساخته می‌شود، حذف از کاتالوگ یعنی حذف از UI.
- **«🤖 پاسخ خودکار» از صفحه‌ی اصلیِ هوش مصنوعی حذف شد** و «🔍 تحلیل متن»
  به ردیفِ بالایی منتقل شد (چیدمانِ ۳ستونه‌ی مرتب). خودِ وظیفه‌ی `auto_reply`
  زنده می‌ماند و ردیفش در «مسیریابیِ وظایف» سرِ جایش است، چون **«💬 چت با
  AI» از همین مسیر استفاده می‌کند** (`ai_router.chat` →
  `try_custom_text("auto_reply", …)`).

### تست
`test_v240_modules.py` هفت ثابتِ تازه گرفت: نبودِ Vision/OCR در کاتالوگ و در
منویِ مسیریابی، routableماندنِ همه‌ی وظایفِ باقی‌مانده (ازجمله `auto_reply`)،
و اینکه هر وظیفه‌ی متنی یا دکمه در صفحه‌ی اصلی دارد یا صراحتاً در فهرستِ
استثناهاست.


## نسخه v2.4.1 — ۱۴۰۵/۰۵/۰۸ (2026-07-30) — دیباگِ سرتاسری و تستِ عملیِ کلِ سورس

یک پاسِ کاملِ دیباگ روی **همه‌ی فایل‌ها** (۴۹ ماژولِ پایتون + `install.sh` +
اکستنشنِ مرورگر) با اجرایِ واقعیِ کد، نه فقط بازخوانی. تمرکز روی سه چیز: نشتِ
اطلاعاتِ حساس، کدی که هیچ‌وقت اجرا نمی‌شد، و شکافِ پوششِ تست.

### 🔐 امنیت
- **`install.sh` توکن و کلیدهای واقعی را هاردکد کرده بود** — توکنِ ربات، آیدیِ
  ادمین، آیدیِ کانالِ مقصد، `MISTRAL_API_KEY` و `GROQ_API_KEY`، به‌علاوه‌ی
  نام/آیدیِ سه کاربر و شش کانالِ مبدأ. همه حذف شد و به الگویِ «موقعِ اجرا
  بپرس» برگشت (با امکانِ پاس‌دادنِ متغیرِ محیطی برای نصبِ بدونِ تعامل:
  `BOT_TOKEN=... ADMIN_IDS=... ./install.sh`).
  ⚠️ اگر این نسخه‌ی `install.sh` جایی منتشر شده، آن توکن/کلیدها را
  **باطل و جایگزین** کن.
- آیدی‌های عددیِ واقعی که به‌عنوانِ «مثال» در متنِ راهنمای منو
  (`handlers/menu.py`) و `tests/rtl_probe.py` مانده بودند با آیدیِ نمونه
  جایگزین شدند.

### رفعِ باگ
- **دکمه‌ی «📊 آمار و زمان‌بندی هوشمند» هیچ کاری نمی‌کرد.** دکمه در
  `keyboards.destination_detail_menu` و کلِ صفحه‌اش در `kb.dst_smart_menu`
  ساخته می‌شد و `SmartScheduler` هم کامل بود، ولی `handlers/menu._dispatch`
  هیچ شاخه‌ای برای `dst:smart:` و `dst:smarttoggle:` نداشت — زدنشان فقط لاگِ
  «callback_data ناشناخته» می‌داد. هر دو وصل شدند (زیرِ همان گیتِ مالکیتِ
  `dst:`، پس هر کاربر فقط روی مقصدِ خودش).
- **`web_search.detect_search_kind` زیررشته‌ای تطبیق می‌داد** — «خبر»
  زیررشته‌ی «خبرنگار»/«خبرگزاری» هم هست، پس «عکس خبرنگار» به‌اشتباه جست‌وجویِ
  خبری می‌شد. حالا فقط کلمه‌ی کامل تطبیق می‌خورد و اگر هر دو نوع کلمه در متن
  بود، هرکدام زودتر آمده برنده است.
- **اعرابِ چسبیده کوئریِ SerpAPI را خراب می‌کرد** — اعراب نویسه‌ی `\w` نیست،
  پس مرزِ کلمه آن را نمی‌گرفت و «عکسِ گربه» بعد از پاک‌سازی به «ِ گربه» تبدیل
  و همان به SerpAPI فرستاده می‌شد. حالا اعراب/کشیده قبل از تطبیق حذف می‌شود.
- **`ai_router` بدونِ کلید هم درخواست می‌ساخت** — هدرِ `Authorization: Bearer `
  خالی باعث می‌شد httpx همان‌جا (قبل از هر تماسِ شبکه‌ای) استثنا بدهد و لاگِ
  گمراه‌کننده‌ی «خطا در فراخوانی Mistral/Groq» بخورد، انگار سرویس خراب است.
  حالا Providerِ بدونِ کلید تمیز رد می‌شود.
- **`install.sh` با فهرستِ خالیِ کاربران/کانال‌ها زیرِ `set -u` می‌ترکید**، و با
  آیدیِ مقصدِ خالی یک مقصدِ بی‌آیدی در دیتابیس می‌ساخت. هر دو گارد شدند.
- `button_config.py`: «🔒 دسترسی‌ها» (`usr:perms:`) تنها دکمه‌ی بی‌رنگِ صفحه‌ی
  جزئیاتِ کاربر بود؛ هم‌رتبه با بقیه `primary` شد.
- `bot/__init__.py`: نسخه روی `2.0.0` جا مانده بود → `2.4.0`.
- `.env.example`: `GEMINI_PROXY_URL` جا افتاده بود.
- پاک‌سازیِ کدِ مرده/importهای بی‌استفاده (`poster.last_exc`،
  `ad_filter.first_provider`، `custom_watermark.uid` و چند import).

### تست
- **`tests/run_all.py` رگرسیون را قورت می‌داد**: فقط کدِ خروج را چک می‌کرد، ولی
  چند فایلِ تست خطا را فقط چاپ می‌کنند و با کدِ ۰ خارج می‌شوند — یعنی چند
  شکستِ واقعی بی‌سروصدا از سوییت رد شده بود. حالا خروجی هم برای نشانه‌ی
  `FAIL` بررسی می‌شود.
- **تستِ جدید `test_menu_smoke.py`**: هر ۳۰۰ `callback_data`ی که سازنده‌های
  کیبورد تولید می‌کنند از خودِ سورس استخراج و با یک دیتابیسِ واقعیِ موقت از
  مسیرِ واقعیِ `_dispatch` اجرا می‌شود. همین تست بود که باگِ «آمار و زمان‌بندیِ
  هوشمند» را پیدا کرد.
- **تستِ جدید `test_database.py`**: اسکیما، idempotent بودنِ مهاجرت‌ها،
  CRUD، تنظیمات، اسلات‌ها، صفِ تایید و claimِ اتمیک، کلیدهای چندتاییِ AI،
  نگه‌داری و cascadeِ حذف.
- **تستِ جدید `test_dedup_cache_backup.py`**: تشخیصِ تکراری، سقفِ بایتیِ کش و
  حسابداریِ حجم، و رفت‌وبرگشتِ کاملِ بکاپ/بازیابی (شاملِ سالم‌ماندنِ دیتابیس بعد
  از بازیابیِ ناموفق).
- **تستِ جدید `test_v240_modules.py`**: `web_search`، `ai_crypto`، سلامتِ
  `ai_catalog` و ثابتِ «یک منبعِ حقیقتِ واحد» برای برچسبِ منویِ AI.
- تست‌های قدیمی‌ای که از خودِ کد عقب مانده بودند به‌روز شدند (امضای سه‌تاییِ
  `llm_classify`، نشانه‌ی `_GAP_MARK` در `append_footer`، کپشنِ ثابتِ کانفیگ
  در تقسیمِ پیام، و سیاستِ فعلیِ رنگِ دکمه‌ها).

نتیجه: `python3 tests/run_all.py` → **ALL PASS (۱۱ ماژول)**، و
`ruff check --select F,E9,PLE` روی کلِ سورس تمیز است.


## نسخه v2.4.0 — ۱۴۰۵/۰۵/۰۸ (2026-07-30) — SerpAPI-only، یکسان‌سازیِ منوها، رفعِ باگِ کپشنِ دو مقصد

### تغییرِ جست‌وجویِ وب — فقط SerpAPI با ۵ کلید و ورودیِ واحد
- **Google Programmable Search (API Key + CX) کاملاً حذف شد.** کلِ جست‌وجو (عکس و خبر) فقط روی SerpAPI کار می‌کند.
- **پنج کلیدِ SerpAPI** قابلِ ورود است (رمزنگاری‌شده در دیتابیس). بعد از **هر سرچ** انتخابِ کلید به‌صورتِ round-robin بینِ کلیدهای فعال جلو می‌رود تا سهمیه‌ی رایگانِ همه یکنواخت مصرف شود. اگر کلیدِ نوبت 429/خطا داد، خودکار کلیدِ بعدی امتحان می‌شود.
- **یک ورودیِ واحد** به‌جای دو دکمه‌ی جدا (عکس/خبر): نوعِ جست‌وجو از خودِ متن تشخیص داده می‌شود (`detect_search_kind`):
  - متنِ حاویِ «عکس/تصویر/کاور/پوستر/…» → فقط عکس، **بدونِ هیچ توضیح/کپشنی** زیرِ عکس‌ها.
  - متنِ حاویِ «خبر/اخبار» → خبر همراهِ **لینکِ سایتِ منبع + تاریخ** و در صورتِ وجود عکسِ خبر.
  - پیش‌فرض (بدونِ کلمه‌ی کلیدی) → عکس.
- **جدیدترین‌ها بالا**: برای عکس `tbs=qdr:y,sbd:1` و برای خبر `sbd:1`.
- کلمه‌ی نوع از کوئریِ ارسالی به SerpAPI حذف می‌شود («عکس گربه» → «گربه»).
- به‌روزرسانیِ UI در `keyboards.py`/`handlers/menu.py`/`handlers/inputs.py`؛ منویِ تنظیمات حالا ۵ خانه‌ی کلید دارد.

### یکسان‌سازیِ نامِ سرویس‌ها (صفحه‌ی اصلیِ هوش مصنوعی ↔ مسیریابیِ وظایف)
- صفحه‌ی اصلیِ هوش مصنوعی حالا برچسب‌هایش را از **همان کاتالوگِ مسیریابیِ وظایف** (`ai_catalog.ALL_TASKS`) می‌خوانَد — یک منبعِ حقیقتِ واحد، تا نام‌ها هیچ‌وقت واگرا نشوند.
- برچسب‌ها یکی شد: `fix_text` → «🩹 اصلاح املا/گرامر»، `edit_image` → «🎨 تغییر استایل عکس».
- چهار سرویسِ گم‌شده به صفحه‌ی اصلی اضافه شد (با هندلر و مسیرِ ورودیِ کامل): **پاسخ خودکار، تحلیل متن، تولید کپشن، تولید عنوان**.

### رفعِ باگِ «کپشن به یک مقصد می‌رسد، به مقصدِ دیگر نه»
- گاردِ جدید در `poster.py::send_post`: اگر متنِ خامِ مبدأ محتوا داشته باشد ولی کپشنِ ساخته‌شده‌ی یک مقصد (به‌خاطرِ تنظیماتِ per-destination مثلِ لیستِ حذفِ عبارت) تهی شود، به‌جای ارسالِ بی‌کپشن، کپشن یک‌بارِ دیگر بدونِ تنظیماتِ اختصاصیِ مقصد ساخته و جایگزین می‌شود تا **در هر دو مقصد کپشن برود**.
- ریشه‌یابی: با تستِ بازتولید ثابت شد `send_post` برای دو مقصدِ هم‌تنظیم همیشه کپشنِ یکسان می‌فرستد (تک‌عکس و آلبوم)؛ منشأِ گم‌شدن، تنظیماتِ متفاوتِ per-destination بود که با این گارد پوشش داده شد.


## نسخه v2.3.0 — ۱۴۰۵/۰۵/۰۶ (2026-07-28) — گسترشِ ابزارهایِ هوش مصنوعی

### افزوده‌شده
- **خلاصه‌سازیِ چندسطحی**: `AIRouter.summarize(text, level)` با سه سطح (فوق‌کوتاه/متوسط/مفصل)، انتخاب از یک منویِ ۳تایی قبل از ارسالِ متن.
- **ترجمه‌ی هوشمند**: `AIRouter.translate_smart(text, target)` — تشخیصِ خودکارِ زبانِ مبدأ + انتخابِ زبانِ مقصد (فارسی/انگلیسی/عربی/خودکار). زبانِ تشخیص‌داده‌شده در ابتدایِ پاسخ نمایش داده می‌شه.
- **اصلاحِ املا و گرامر**: `AIRouter.fix_text(text)` — فقط تصحیحِ نگارشی، بدونِ بازنویسیِ سبک (جدا از ابزارِ «بازنویسی»).
- **تولیدِ هشتگ**: `AIRouter.generate_hashtags(text)` — قبلاً فقط در کاتالوگ تعریف شده بود، حالا در منویِ سرویس‌هایِ AI هم در دسترسه.
- **پرامپت‌نویس تصویر**: `AIRouter.prompt_writer(idea)` — تبدیلِ یک ایده‌ی کوتاهِ فارسی به پرامپتِ ساختاریافته‌ی انگلیسی (Subject/Style/Lighting/Composition/Color/Mood/Details/Quality/Negative/Aspect ratio)، طبقِ اصولِ Prompt Engineering.
- **تغییرِ استایلِ عکس**: قابلیتِ ویرایشِ تصویرِ ورودی (نه فقط تولیدِ تصویرِ تازه) از طریقِ Gemini چندوجهی — `ai_adapters._gemini_image_call` حالا `input_image` می‌گیره، `ai_provider_manager.call_image`/`try_custom_image` و `image_router.edit_image` این مسیر رو تا منو وصل می‌کنن. ۶ پریستِ آماده (نقاشیِ رنگ‌روغن/سیاه‌قلم/سایبرپانک/انیمه/سه‌بعدیِ کارتونی/وینتیج) + توضیحِ دلخواه.
- منویِ سرویس‌هایِ AI بازطراحی شد (چیدمانِ ۳ستونه)؛ دکمه‌ی «وضعیتِ AI» از منویِ اصلیِ AI به داخلِ «مدیریتِ API هوش مصنوعی» منتقل شد.
- **جست‌وجویِ وب برایِ تصویر/خبر** (`bot/web_search.py`، ماژولِ کاملاً جدید): کاورِ فیلم/محصول یا خبرِ باعکس‌وتصویر، با استراتژیِ دوموتوره‌یِ Google Programmable Search + SerpAPI:
  - جست‌وجویِ تصویر: اول Google (searchType=image، دقتِ بالا)، فالبک/تکمیل با SerpAPI (google_images)؛ نتایج بر اساسِ رزولوشن رتبه‌بندی و بر اساسِ دامنه دی‌داپلیکیت می‌شن (حداکثر ۲ از هر دامنه، حذفِ عکس‌هایِ زیرِ ۴۰۰px).
  - جست‌وجویِ خبر: اول SerpAPI (google_news، چون تصویر+متن+منبع+تاریخ رو یک‌جا می‌ده)، فالبک به Google (جست‌وجویِ وبِ مرتب‌شده بر اساسِ تاریخ + یک جست‌وجویِ تصویرِ مکمل).
  - کلیدها (Google API Key، Google CX، SerpAPI Key) رمزنگاری‌شده (با همون `ai_crypto` که برایِ کلیدهایِ AI استفاده می‌شه) و از داخلِ منو («🔎 جست‌وجویِ وب» ← «⚙️ تنظیمِ کلیدها») ذخیره می‌شن، بدونِ نیاز به `.env`.
  - هر دو Provider رایگانن: Google ۱۰۰ کوئری/روز، SerpAPI ۱۰۰ کوئری/ماه.

### شناخته‌شده / نیازمندِ تصمیمِ کاربر
- تغییرِ استایلِ عکس فقط با کلیدِ Gemini کار می‌کنه (تنها Providerِ چندوجهیِ فعلیِ پروژه)؛ اگه کلیدِ Gemini تنظیم نشده باشه، پیامِ روشن نمایش داده می‌شه.
- کلیدهایِ Google/SerpAPI باید دستی از پنلِ خودِ اون سرویس‌ها گرفته بشن (Google Cloud Console + Programmable Search Engine، و serpapi.com) و از داخلِ منو ثبت بشن.

## نسخه v2.2.1 — ۱۴۰۵/۰۵/۰۶ (2026-07-28) — دیباگِ عمومی (Extension API + مدیریتِ اعلان‌ها)

بازبینیِ کلیِ کد (AI، Poster، فیلترِ تبلیغات، Scraper، و ماژول‌های جانبی) که
شاملِ خوندنِ خط‌به‌خط + چند اسکریپتِ آنالیزِ استاتیکِ سفارشی (mutable default،
bare except، mismatch فرمت‌استرینگِ لاگ، کراس‌چکِ همه‌ی state هایِ `awaiting`
و `callback_data`) بود. اکثرِ کد از دورهای قبلی تمیز بود؛ دو باگِ واقعی پیدا
و فیکس شد:

### فیکس‌شده

| فایل | باگ | خلاصه |
|---|---|---|
| `bot/extension_api.py` | نشتِ دیسک | توی `_handle_post` (آپلودِ آلبوم از اکستنشنِ مرورگر)، اگه یکی از فایل‌های وسطِ آلبوم (نه لزوماً اولی) از سقفِ ۲۰۰ مگابایت رد می‌شد، تابع مستقیماً `return` می‌کرد و فایل‌هایی که *قبلِ* همون فایل با موفقیت ذخیره شده بودن (توی `saved_paths`) هیچ‌وقت پاک نمی‌شدن — چون این یک `return` مستقیم بود، نه یک exception، پس بلاکِ `except` (که پاک‌سازی رو انجام می‌ده) اصلاً اجرا نمی‌شد. نتیجه: هر آپلودِ آلبومیِ ناموفق، چند فایلِ یتیم برای همیشه روی دیسک جا می‌ذاشت. فیکس: قبل از `return`، همه‌ی `saved_paths` هم پاک می‌شن. |
| `bot/notification_manager.py` | پیامِ HTML‌نامعتبر (اعلان‌های خطا/هشدار/موفقیت بی‌صدا گم می‌شدن) | `notify_error` / `notify_warning` / `notify_success` متن‌های خام (`error_type`، مقادیرِ `details`، نامِ کاربر/کانال، `message`) رو بدونِ escape مستقیم توی پیامِ `parse_mode=HTML` می‌ذاشتن. اگه هر کدوم از این مقادیر (که اغلب از متنِ خطا/استثنایِ یک کتابخانه‌ی خارجی میان) کاراکترِ `<`/`>`/`&` داشت، تلگرام با خطای «Can't parse entities» کلِ پیام رو رد می‌کرد و `send_admin_notification` این خطا رو فقط لاگ می‌کرد — یعنی دقیقاً همون اعلانی که قرار بود ادمین رو از یک خطا باخبر کنه، بی‌صدا گم می‌شد. این دقیقاً همون کلاس‌باگی‌ست که قبلاً در `ai_provider_manager.py` و `poster.py._notify_admins_of_failures` فیکس شده بود؛ اینجا جا افتاده بود. فیکس: همه‌ی مقادیرِ متغیر با `html.escape` قبل از قرار گرفتن در پیام escape می‌شن. (نکته: این سه تابع فعلاً در هیچ‌جای پروژه صدا زده نمی‌شن — باگِ نهفته‌ای بود که با اولین استفاده‌ی آینده از این توابع خودشو نشون می‌داد.) |

### بررسی شد و باگ نبود (برای شفافیت)
- `send_post` در `poster.py` (خطِ اصلیِ ارسال، ~۵۰۰ خط): آلبوم/عکس/ویدیو/فایل/متنِ تقسیم‌شده، fallback هایِ دانلود، و منطقِ «موفقیت = واقعاً چیزی ارسال شد» — همه درست.
- `database.py`: کوئری‌هایِ داینامیکِ `UPDATE ... SET {col}=?` از یک whitelistِ ستونِ ثابت می‌سازن، نه ورودیِ کاربر — امن.
- `backup_manager.py`: ری‌استورِ جدول‌به‌جدول با savepoint و دیفِ ستون‌ها بینِ بکاپ/اسکیمایِ فعلی — درست.
- `duplicate_filter.py`، `custom_watermark.py`، `watermark.py`، `formatter.py` (`strip_phrases_html`/`_balance_html`)، `cache.py`، `concurrency.py`، `smart_scheduler.py`، `auto_poster/*`: بدونِ باگ.
- کراس‌چکِ خودکارِ همه‌ی state هایِ منو (`awaiting`) و `callback_data`: هیچ حالتِ یتیم/گم‌شده‌ای نبود.
- **تکمیلِ بازبینی (این دور):** `handlers/menu.py` (کاملِ ۳۰۷۲ خط، شاملِ همه‌ی هندلرهای `callback_router`/`_dispatch`، منوهای متنی، زنجیره‌ی مجوزها) و `keyboards.py` (کاملِ ۱۱۰۹ خط، همه‌ی سازنده‌های `InlineKeyboardMarkup`/`ReplyKeyboardMarkup`) که در دورِ قبل هنوز کامل خونده نشده بودن، این‌بار خط‌به‌خط بررسی شدن: escapeِ HTML روی همه‌ی مقادیرِ داینامیک (تایتلِ کانال/کاربر، هندل، متنِ دلخواه) قبلِ قرار گرفتن در پیام‌هایِ `parse_mode=HTML` سازگار و کامله؛ زنجیره‌ی چکِ مجوز/مالکیت در `_dispatch` (بخشِ `not is_admin`) روی همه‌ی پیشوندهایِ `src:`/`dst:`/`usr:`/`wmp:`/`adf:`/`fmt:`/`footer:`/`aiapi:` درست اعمال می‌شه و هیچ پیشوندی از زیرِ گیت در نرفته؛ کراس‌چکِ دستیِ همه‌ی `callback_data`ای که `keyboards.py` می‌سازه در برابرِ هندلرهایِ `menu.py` هیچ حالتِ یتیم/گم‌شده‌ای نشون نداد؛ صفحه‌بندی (`_paginate`/`_page_nav_row`) و مدیریتِ استیتِ `awaiting`/`nav_back` هم در همه‌ی مسیرها consistent بودن. باگِ جدیدی پیدا نشد.

## نسخه v2.2.0 — ۱۴۰۵/۰۵/۰۷ (2026-07-28) — چندکلیدیِ AI با چرخشِ خودکار

### ویژگیِ جدید
هر سرویسِ هوش مصنوعی (Mistral، Groq، Gemini، Hugging Face) حالا می‌تونه
به‌جایِ یک کلید، **تا ۵ کلیدِ API** (مثلاً از ۵ اکانتِ جداگانه) داشته باشه.
کاربردِ اصلی: وقتی چند اکانتِ Gemini/Groq/... می‌سازی و از هرکدوم یک کلیدِ
رایگان می‌گیری، دیگه لازم نیست دستی جابه‌جا کنی — ربات خودکار مدیریتشون
می‌کنه.

**رفتارِ چرخش:**
- درخواست‌ها به‌طورِ چرخشی (round-robin) بینِ کلیدهایِ فعالِ هر سرویس پخش
  می‌شن — بعدِ هر فراخوانیِ موفق، نوبت می‌ره سراغِ کلیدِ بعدی.
- اگه یک کلید همون لحظه به سقفِ Quota بخوره (خطایِ 429)، بدونِ اینکه کاربر
  چیزی ببینه، در همون درخواست خودکار به کلیدِ بعدی سوییچ می‌کنه.
- کلیدی که Quota تمام کرده به‌مدتِ ۱۵ دقیقه از چرخه کنار گذاشته می‌شه و
  بعدش خودکار دوباره امتحان می‌شه (بدونِ نیاز به Retest دستی).
- این منطق در یک نقطه‌ی مرکزی (`ai_provider_manager.call_text` /
  `call_image`) پیاده شده، پس با **همه‌یِ وظایف** (فیلترِ هوشمندِ تبلیغات،
  خلاصه‌سازی، بازنویسی، اصلاحِ متن، ترجمه، تولیدِ عنوان/کپشن/هشتگ، پاسخِ
  خودکار، تحلیلِ متن، تولید/ویرایشِ تصویر، Vision/OCR) که از طریقِ
  `AIRouter`/`image_router` مسیریابی می‌شن هماهنگه — بدونِ نیازِ تغییر در
  اون بخش‌ها.

### دیتابیس — `database.py`
جدولِ جدیدِ `ai_provider_keys` (owner_user_id, service_id, slot ۱ تا ۵،
کلیدِ رمزنگاری‌شده، وضعیت، آمارِ مستقل به‌ازایِ هر کلید، `cooldown_until`).
مهاجرتِ خودکار: کلیدِ تک‌اسلاتیِ قدیمیِ هر سرویس (اگه از قبل ثبت شده بود) به
slot ۱ منتقل می‌شه — چیزی از دست نمی‌ره. ستونِ `rotation_cursor` هم به
`ai_providers` اضافه شد (نگه‌داریِ نوبتِ چرخش).

### منویِ مدیریتِ API — `handlers/ai_providers_menu.py`
صفحه‌ی هر سرویس حالا تعدادِ کلیدها (مثلاً «۳/۵») و وضعیتِ تک‌تکشون رو نشون
می‌ده؛ دکمه‌ی «➕ افزودنِ کلید» (تا سقفِ ۵ تا) و صفحه‌ی «🗂 مدیریتِ کلیدها»
برایِ Retest/حذفِ تک‌تکِ کلیدها اضافه شد. «🔄 بررسیِ اتصالِ همه» همه‌یِ
کلیدهایِ یک سرویس رو یک‌جا تست می‌کنه.

## نسخه v2.1.3 — ۱۴۰۵/۰۵/۰۶ (2026-07-28) — واچ‌داگِ مستقلِ shutdown

### مشکل
دو سقفِ زمانیِ موجود (تایم‌اوتِ ۱۲ثانیه‌ایِ `validate_provider()` در
`ai_provider_manager.py` و گریسِ ۱۰ثانیه‌ایِ بستنِ ThreadPoolExecutor در
`_post_shutdown`ِ `main.py`) روی مسیرِ shutdown **پشتِ‌سرِ‌هم** اجرا می‌شن، نه
هم‌زمان. جمعِ بدترین‌حالتشون (۱۲+۱۰=۲۲ ثانیه) از سقفِ `TimeoutStopSec=20`ِ
یونیتِ systemd (`install.sh`) رد می‌شه؛ در نتیجه در این سناریو باگِ اصلی
(«State 'stop-sigterm' timed out. Killing.» + SIGKILLِ خشنِ کلِ cgroup توسطِ
systemd) هنوز می‌تونست رخ بده.

### فیکس — `main.py`
افزودنِ `_spawn_shutdown_watchdog()`: یک پروسه‌ی بچه‌یِ مستقل که با
`os.fork()`، قبل از هر importِ سنگین/threadی، ساخته می‌شه. بعد از دریافتِ
SIGTERM/SIGINT، اگه پروسه‌ی اصلی ظرفِ ۱۶ ثانیه (`_WATCHDOG_GRACE_SECONDS`،
زیرِ سقفِ ۲۰ثانیه‌ای) خودش تمیز نمرده باشه، مستقیماً با SIGKILL می‌کشتش —
مستقل از اینکه گیرکردن کجای زنجیره‌ی shutdown اتفاق افتاده. همچنین fd
inheritance (stdin/stdout/stderrِ به‌ارث‌رسیده از fork) در پروسه‌ی بچه بسته و
به `/dev/null` وصل می‌شه تا واچ‌داگ pipeِ journald رو دستش نگه نداره.

تستِ دستی (دو سناریو، خارج از این ریپو): پروسه‌ی اصلیِ hangکرده (SIGTERM
ignore‌شده) دقیقاً در `۱۶.۰۳` ثانیه با SIGKILL کشته شد؛ پروسه‌ی اصلیِ سالم در
`۰.۰۲` ثانیه بدونِ دخالتِ واچ‌داگ خارج شد. fdهای پروسه‌ی واچ‌داگ (از طریقِ
`/proc/<pid>/fd`) تأیید شد که به `/dev/null` وصل‌ان، نه به pipeِ اصلی.

## نسخه v2.1.0 — ۱۴۰۵/۰۵/۰۳ (2026-07-25)
### اصلاحِ باگ‌های فاجعه‌بار و بحرانی (بدونِ تغییرِ رفتارِ کاربریِ عادی)

این نسخه فقط باگ‌های **فاجعه‌بار (۴)** و **بحرانی (۱۰)** را اصلاح می‌کند. هیچ
توکن/آیدی/کلیدی تغییر نکرده است. هر تغییر در خودِ کد کامنتِ فارسی با کدِ باگ
(C1..C4 / R1..R10 / M8..M10) دارد.

### فایل‌های تغییریافته (۱۱ فایل)

| فایل | باگ‌های اصلاح‌شده | خلاصه |
|---|---|---|
| `bot/backup_manager.py` | C1, C2, R5, R6, M8 | SAVEPOINT به‌ازای هر جدول (جلوگیری از از دست رفتنِ داده)؛ ترتیبِ درستِ commit سپس PRAGMA FK؛ مسیرِ پوشه‌ی بکاپِ مقاوم؛ اصلاحِ محاسبه‌ی checksum؛ قفلِ سراسری روی بازیابی |
| `bot/cache.py` | C3, M10 | سقفِ بایتیِ کش؛ تصحیحِ نشتِ `_total_size`؛ اصلاحِ دوباره‌شماریِ کلیدِ تکراری |
| `bot/config.py` | C3, C4 | افزودنِ `DOWNLOAD_CACHE_MAX_BYTES` و `MAX_DOWNLOAD_BYTES` |
| `bot/scheduler.py` | C3 | ثبتِ jobِ دوره‌ایِ پاکسازیِ کشِ منقضی |
| `bot/poster.py` | C4, R3 | دانلودِ استریمی با سقفِ حجم؛ استفاده از دِدوپِ اتمیک |
| `bot/duplicate_filter.py` | R2, R3, R9 | اصلاحِ فرمتِ زمانِ پنجره‌ی ۲۴ساعته؛ متدِ اتمیکِ چک‌وثبت؛ خواندنِ threshold خارج از قفل |
| `bot/extension_api.py` | R1 | پاکسازیِ فایل‌های آپلودی در مسیرِ موفق (finally) |
| `bot/smart_scheduler.py` | R4 | قفلِ سراسری روی نوشتن؛ type-hintِ درست؛ مستندسازیِ محدودیتِ API |
| `bot/database.py` | R4 | افزودنِ `UNIQUE(destination_id, hour)` به `channel_stats` |
| `bot/resource_monitor.py` | R7 | dictِ کاملِ امن در مسیرِ خطا (رفعِ KeyError)؛ CPU sampling غیرمسدودکننده |
| `bot/public_report_channel.py` | R8 | نگه‌داشتنِ رفرنسِ تسکِ retry (رفعِ Task GC) |
| `install.sh` | R10 | رفعِ کدِ مرده‌ی `$?` بعد از `set -e`؛ محدودسازیِ مجوزِ `.env`/دیتابیس به ۶۰۰ |

### جزئیاتِ فنی

**🔴 فاجعه‌بار**
- **C1** — بازیابیِ بکاپ: هر جدول داخلِ SAVEPOINT مستقل. قبلاً خطای یک جدول بعد از
  `DELETE` با commitِ نهایی، جدول را برای همیشه خالی می‌کرد.
- **C2** — `PRAGMA foreign_keys=ON` حالا بعد از `commit()` اجرا می‌شود (قبلاً داخلِ
  تراکنش no-op بود و FK تا آخرِ عمرِ کانکشن خاموش می‌ماند).
- **C3** — کش: سقفِ بایتی + پاکسازیِ دوره‌ای + رفعِ نشتِ حساب‌داری. قبلاً تا ۱.۶GB رم.
- **C4** — دانلودِ استریمی با سقفِ ۶۰MB (پیش‌فرض). قبلاً کلِ فایل در رم ⇒ OOM.

**🟠 بحرانی**
- **R1** پرشدنِ دیسک از فایل‌های آپلودیِ پاک‌نشده — رفع شد.
- **R2** ناسازگاریِ فرمتِ زمان (`T` در برابرِ فاصله) که پنجره‌ی ۲۴ساعته‌ی تشخیصِ
  تکراری را می‌شکست — رفع شد.
- **R3** شرطِ رقابتیِ پستِ تکراری بین کانال‌های موازی — چک‌وثبتِ اتمیک.
- **R4** «زمان‌بندیِ هوشمند»: قفل + UNIQUE اضافه شد. (توجه: این قابلیت بدونِ منبعِ
  بازدید عملاً داده‌ای ندارد؛ Bot API تلگرام بازدیدِ پیام نمی‌دهد — محدودیتِ API.)
- **R5** مسیرِ بکاپ و پاکسازیِ فایل‌های قدیمی — رفع شد.
- **R6** هشدارِ همیشگیِ mismatchِ checksum — رفع شد.
- **R7** KeyError در مانیتورِ منابع هنگام خطا + بلاک‌شدنِ event loop — رفع شد.
- **R8** نشتِ تسکِ retryِ گزارشِ عمومی — رفع شد.
- **R9** دِدلاکِ نهفته (خواندنِ setting داخلِ قفل) — رفع شد.
- **R10** کدِ مرده در `install.sh` + مجوزِ ناامنِ `.env` — رفع شد.

### باگ‌هایی که عمداً اصلاح نشدند (خارج از دامنه‌ی فاجعه‌بار/بحرانی)
۲۵ موردِ «متوسط» و ۱۱ گروهِ «جزئی» (مثلِ `except: pass`ها، APIهای منسوخ،
type-hintهای جزئی) در گزارشِ ممیزی مستند شده‌اند ولی طبقِ توافق در این دور اصلاح
نشدند تا ریسکِ رگرسیون کمینه بماند. با دستورِ صریح می‌توان دورِ بعدی رویشان کار کرد.

---

## نسخه v2.1.1 — ۱۴۰۵/۰۵/۰۳ (2026-07-25) — دورِ دومِ اصلاحات (متوسط)

### توضیحِ صادقانه درباره‌ی «۲۵ موردِ متوسط»
عددِ «۲۵» در جدولِ خلاصه‌ی گزارشِ اولیه یک **برآوردِ دسته‌ای** بود، نه فهرستِ
مستند. در بازبینیِ دقیق‌ترِ این دور مشخص شد:
- بخشی از آن‌ها در همان دورِ اول به‌عنوانِ بحرانی اصلاح شده بودند (R4–R10).
- چند موردِ حدس‌زده‌شده **اصلاً باگ نبودند** و عمداً اصلاح نشدند (نمونه: الگوی
  `get_settings()` که به‌نظر می‌رسید defaultها را دور می‌زند، ولی همه‌ی
  فراخوان‌ها از `.get(key, default)` استفاده می‌کنند؛ و نشتِ فرضیِ `AIRouter`
  که در واقع در `finally` بسته می‌شود).
- بنابراین به‌جای ساختنِ ۲۵ اصلاحِ مصنوعی، فقط باگ‌های متوسطِ **واقعی و
  تأییدشده** اصلاح شدند.

### اصلاح‌شده در این دور
| فایل | باگ | خلاصه |
|---|---|---|
| `bot/poster.py` | M1 | `_RETRYABLE_ERROR_MARKERS` تعریف شده بود ولی استفاده نمی‌شد؛ خطاهای دائمی (chat not found، blocked، not enough rights و...) ۳ بار بی‌فایده retry می‌شدند. حالا فقط خطاهای گذرا/ناشناخته retry می‌شوند و خطاهای دائمیِ شناخته‌شده بلافاصله رها می‌شوند. |
| `bot/scheduler.py` | M2 | `_warning_targets` وقتی `approval_chat_id` تهی بود `[None]` برمی‌گرداند و `send_message(chat_id=None)` می‌ترکید. حالا به `telegram_id` و سپس ADMIN_IDS fallback می‌کند. |

### موردِ متوسطی که عمداً اصلاح *نشد* (با استدلال)
- **auto_poster/scheduler ad exact-minute match**: تیکِ ۲۰ثانیه‌ای با شرطِ
  `now_hhmm == t`. فقط اگر یک تیک بیش از ۶۰ ثانیه بلاک شود تبلیغ آن دقیقه از
  دست می‌رود (احتمالِ بسیار کم). «اصلاحِ» آن با پنجره‌ی since-last-tick ریسکِ
  **ارسالِ دوبارهٔ تبلیغ** را ایجاد می‌کند که بدتر از یک از دست رفتنِ نادر است.
  طبق اصلِ «هر اصلاح نباید رفتارِ بدتری بسازد» دست‌نخورده ماند و صرفاً مستند شد.

### باقی‌مانده (دستهٔ متوسط/جزئی)
۳۳ موردِ `except: pass` و ۳۲ موردِ except بدونِ لاگ در گزارش مستندند. اکثرِ
این‌ها **عمدی و بی‌خطرند** (لاگ/پاکسازیِ best-effort که نباید جریانِ اصلی را
بترکاند). اصلاحِ یک‌جای همهٔ آن‌ها ارزشِ ریسکِ رگرسیون را ندارد؛ در صورتِ نیاز
می‌توان مورد‌به‌مورد بررسی کرد.

---

## نسخه v2.1.2 — ۱۴۰۵/۰۵/۰۳ (2026-07-25) — دیباگِ حرفه‌ایِ scraper.py + وابسته‌ها

### روش
`scraper.py` خط‌به‌خط بازخوانی شد و منطقِ آن با **۹ تستِ واقعی روی HTMLِ نمونه**
(تک‌پست، آلبومِ چندتایی، بلاکِ ریپلای، ویدیوی حل‌نشده، سند/موزیک/ویس، data-postِ
نامعتبر) و **۳ تستِ صفحه‌بندی/ضدِ حلقه‌ی بی‌نهایت** تأیید شد. همه‌ی مصرف‌کننده‌های
API اسکرِیپر (scheduler، poster، extension_api، menu، cli، backup_manager) از نظرِ
تطابقِ امضا و قرارداد چک شدند.

### نتیجه
- ساختارِ پارس (متن/عکس/ویدیو/سند/صوت/ویس)، جداسازیِ بلاکِ ریپلای، ترکیبِ آلبوم
  زیرِ بزرگ‌ترین آیدی، و بازیابیِ ویدیو از صفحه‌ی امبد — **همگی درست کار می‌کنند،
  باگی یافت نشد.**
- تطابقِ نوعِ مدیا بینِ تولید (scraper) و مصرف (poster: photo/video/document/
  audio/voice) کامل است.
- آرگومان‌ها و قراردادِ همه‌ی توابعِ عمومیِ اسکرِیپر با همه‌ی فراخوان‌ها سازگارند.

### اصلاح‌شده
| فایل | باگ | خلاصه |
|---|---|---|
| `bot/scraper.py` | S3 | نگهبانِ ضدِ گیرکردن روی یک صفحه در `fetch_new_posts`: اگر `before_id` جلو نرود، به‌جای ۱۵ درخواستِ بی‌فایده تا `max_pages`، بلافاصله متوقف می‌شود. (در حالتِ عادیِ t.me/s رخ نمی‌داد، ولی نگهبانِ ایمنی/کارایی است.) |

### مواردی که بررسی شد و **باگ نبود** (شفافیت)
- کشِ `_embed_fail_cache` (dictِ ساده): چون همه‌ی فراخوان‌های اسکرِیپر روی همان
  حلقه‌ی asyncio اجرا می‌شوند (نه ترد)، شرطِ رقابتی ندارد.
- نبودِ سقفِ حجم روی `resp.text`: منبع همیشه telegram.org با صفحه‌ی محدود است؛
  ریسکِ عملی ندارد و افزودنِ محدودیت می‌توانست پارس را بشکند.
- مسیرِ retryِ 5xx و بازگشتِ `resp=None`: هر دو درست مدیریت شده‌اند.

## فیکسِ ارورهای گزارش‌شده (کالبک‌کوئریِ منقضی + پیامِ گمراه‌کننده‌ی webpage_media_empty)

**۱. کرش/اسپمِ «یه خطای غیرمنتظره پیش اومد» + لاگِ `Query is too old and response timeout expired or query id is invalid`**

فیکسِ قبلی فقط answerِ خالیِ انتهایی‌ی `callback_router` رو امن کرده بود؛ اما
ده‌ها جایِ دیگه (توی همین فایل، `manual_poster.py`، `auto_poster/menu.py`،
`custom_watermark.py`) مستقیماً `query.answer("...", show_alert=True)` صدا
می‌زنن بدونِ try/except. وقتی همین answerِ اول (نه اون آخری) به‌خاطرِ کند
بودنِ پردازش یا دابل‌تپ‌کردنِ کاربر با خطا مواجه می‌شد، این استثنا از
try/finally رد می‌شد و مستقیم به هندلرِ سراسریِ خطا می‌رسید: هم تریس‌بکِ
تکراری لاگ می‌شد، هم پیامِ «یه خطای غیرمنتظره پیش اومد. دوباره امتحان کن یا
لاگ رو چک کن.» توی چتِ کاربر/کانالِ تایید اسپم می‌شد - درحالی‌که خودِ
عملیات (مثلاً تاییدِ پست) معمولاً از قبل با موفقیت انجام شده بود.

فیکس: یک تابعِ کمکیِ `safe_answer` + `is_expired_callback_query_error` توی
`bot/handlers/common.py` اضافه شد که این خطای خاص رو قورت می‌ده (فقط لاگِ
سبک می‌کنه)، بدونِ اینکه خطاهای واقعیِ دیگه رو پنهان کنه. `callback_router`
حالا کلِ `_dispatch` رو توی این محافظت می‌پیچه، پس هر جایی از زنجیره‌ی
دیسپچ (npz/manual/wmc/pp/...) که به این خطای خاص بخوره، بی‌سروصدا نادیده
گرفته می‌شه.

**۲. پیامِ ناموفقیِ ارسال به مقصد همیشه می‌گفت «ادمینش کن، خودکار دوباره
تلاش می‌شه»**

این پیام برایِ *همه‌ی* خطاها (از جمله `webpage_media_empty`، که یعنی
تلگرام نتونسته عکسِ پیش‌نمایش/بندانگشتیِ لینکِ پست رو بگیره و هیچ ربطی به
دسترسیِ ادمین نداره) یکسان نشون داده می‌شد؛ و حتی برای خطاهایِ واقعاً
مربوط‌به-ادمین هم وعده‌ی «دوباره‌ی خودکار» می‌داد، درحالی‌که این پستِ خاص
اگه حداقل یک مقصدِ دیگه موفق شده باشه، دیگه هیچ‌وقت خودکار دوباره فرستاده
نمی‌شه (last_post_id جلو رفته). فیکس: `_notify_admins_of_failures` توی
`bot/poster.py` حالا بر اساسِ متنِ خودِ خطا راهنماییِ متفاوت می‌ده و
دیگه وعده‌ی نادرست نمی‌ده.

## فیکسِ `mrliq-bot.service: Failed with result 'timeout'`

این دقیقاً همون کلاسِ باگی‌یه که قبلاً توی همین فایل (`main.py`) مستندش
کرده بودیم: یک ThreadPoolExecutorِ non-daemon برایِ کارهایِ سنگین (واترمارک/
بهبودِ کیفیت با AI) هست که فیکسِ قبلی‌ش (`_concurrency.shutdown()` با
`wait=True` توی `_post_shutdown`) از idle موندنِ همیشگیِ تردها جلوگیری
می‌کرد - ولی یک حفره داشت: اگه دقیقاً لحظه‌ی `systemctl stop` یک پردازشِ
سنگین (مثلاً بهبودِ کیفیتِ تصویر با torch روی CPU) واقعاً درحالِ اجرا باشه،
هیچ راهی برایِ لغوِ اون ترد از بیرون نیست؛ `wait=True` تا تمومِ واقعیِ اون
کار صبر می‌کنه - که می‌تونه از `TimeoutStopSec` یونیتِ systemd (۲۰ ثانیه)
بیشتر طول بکشه و دقیقاً همون `Failed with result 'timeout'` رو دوباره
بده.

فیکس: توی `_post_shutdown`، انتظار برایِ بستنِ Executor حالا یک سقفِ زمانیِ
امن داره (`_HEAVY_SHUTDOWN_GRACE_SECONDS = 10` ثانیه، واضحاً کمتر از
TimeoutStopSecِ ۲۰ثانیه‌ی install.sh). اگه از این سقف رد بشه (یعنی کارِ
سنگین هنوز تموم نشده)، به‌جایِ این‌که بذاریم systemd دیر یا زود با SIGKILL
همه‌چی رو بکشه، خودمون با `os._exit(0)` یه خروجِ فوری و کنترل‌شده انجام
می‌دیم؛ کارِ نیمه‌کاره از دست می‌ره ولی `Restart=on-failure` بلافاصله
دوباره بالاش می‌آره - به‌جایِ ده‌ها ثانیه معطلی و لاگِ زشتِ SIGKILL.

⚠️ نکته: این فیکس فقط با جایگزینیِ `main.py` روی سرور اعمال می‌شه؛ فایلِ
یونیتِ systemd (`/etc/systemd/system/mrliq-bot.service`) که از قبل نصب
شده دست‌نخورده می‌مونه (چون از رویِ `install.sh` فقط موقعِ نصبِ اولیه ساخته
می‌شه) - نیازی به تغییرش نیست، چون سقفِ داخلیِ ۱۰ ثانیه‌ایِ ما به‌اندازه‌ی
کافی زیرِ TimeoutStopSecِ ۲۰ثانیه‌ی موجودش هست.
