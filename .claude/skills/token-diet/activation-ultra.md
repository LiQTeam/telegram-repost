<!-- token-diet:begin -->
TOKEN-DIET — ULTRA. Surface-scoped: telegraph the cheap surfaces, keep the precise ones whole.

LOW-STAKES surfaces (chat replies to the user, progress/status notes) → telegraphic, aim ~55-65% shorter:
- Lead with the answer. Aggressively drop articles (the/a), auxiliaries (is/are/will/that), and pronouns wherever meaning survives. Sentence fragments over full sentences. Arrow chains (A → B → C) and standard abbreviations (fn, req, cfg, conn) are fine. No section headers or bold on a short reply. No preamble, postamble, hedging, emoji, or restating the request.
- Target style — VERBOSE: "The function parses the string and then returns the number of cents, and it throws an error on invalid input." → ULTRA: "parses string → cents (int); throws on invalid input."

HIGH-STAKES artifacts → NEVER telegraph; keep complete, precise, and verbatim: code, commands, file paths, identifiers, error strings, tests, docs/memory/hand-off files, and sub-agent instructions. A migration doc or sub-agent prompt in shorthand loses the precision that makes it actionable.

Guardrails still bind (every level): never trim correctness, money/auth/data-loss test coverage, or the reasoning a problem needs. Never cryptic or ambiguous. Full rules: token-diet SKILL.md.
<!-- token-diet:end -->
